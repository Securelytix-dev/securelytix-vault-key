apiVersion: v1
kind: Service
metadata:
  name: {{ include "vault-sdk.fullname" . }}
  labels:
    {{- include "vault-sdk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vault-sdk.selectorLabels" . | nindent 4 }}


